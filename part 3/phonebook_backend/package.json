{
  "name": "phonebook_backend",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "author": "Judith Rea",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd ../../part\\2/phonebook && npm run build && cp -r dist ../../part\\3/phonebook_backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.1.0",
    "morgan": "^1.10.1"
  }
}
